import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.fala-comigo',
  appName: 'fala-comigo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
